process MONITOR = { 

     module ecalDbMonitor = EcalBarrelMonitorDbModule{
       untracked string prefixME = "EcalBarrel"
       untracked string htmlDir = "."
       untracked string xmlFile = "test.xml"
       untracked int32  sleepTime = 2
     }

     path p = { ecalDbMonitor }	

     untracked PSet maxEvents = {untracked int32 input = -1}
     source = DummySource{}

include "DQMServices/Core/data/DQM.cfg"

     replace DQM.collectorHost = ""

     service = MessageLogger{
       untracked vstring destinations = { "cout" }
       untracked PSet cout = { untracked string threshold = "INFO"
         untracked bool noTimeStamps = true
         untracked bool noLineBreaks = true
                             }
     }

}
